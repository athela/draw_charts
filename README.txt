功能说明：
    生成跳绳打卡表格；以年历的形式，在每个方格里可以记录跳绳个数；可以用不同颜色的笔画圈，一眼看到表格中不同数量段的颜色；
    生成的图表结果在 output_charts 目录中；

运行操作：
    在 draw_chart_config.json 中自定义月份范围;
    运行 main_draw_calendar.py, 生成表格存放在 output_charts 目录；